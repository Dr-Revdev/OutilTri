    private TabControl tabs;
    private TabPage tabTri, tabParams;
    private DataGridView gridRules;
    private Button btnRulesReload, btnRulesSave;
    private Dictionary<string, string> rules = new(StringComparer.OrdinalIgnoreCase);
    private readonly string rulesPath = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "rules.json");

    public MainForm()
    {
        //TabControl
        tabs = new TabControl { Dock = DockStyle.Fill };
        tabTri = new TabPage("Tri");
        tabParams = new TabPage("paramètres");
        tabs.TabPages.Add(tabTri);
        tabs.TabPages.Add(tabParams);
        Controls.Add(tabs);
